# Test environment variables
# Dummy values so tests can run without real API credentials.
# MSW and per-test fetch mocks intercept all HTTP requests,
# so these tokens are never sent to real APIs.

HARDCOVER_API_TOKEN=test-token-for-ci
